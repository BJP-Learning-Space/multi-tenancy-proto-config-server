server:
  port: 80
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BJP-Learning-Space/multi-tenancy-config
          search-paths: /**
          default-label: main
